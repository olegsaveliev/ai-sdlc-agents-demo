name: QA Agent - Auto Test Generation

on:
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch:

jobs:
  qa-agent:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.ref }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install anthropic requests pytest dnspython
          
          # Install app dependencies if requirements.txt exists
          if [ -f "requirements.txt" ]; then
            echo "📦 Installing from requirements.txt..."
            pip install -r requirements.txt
          fi

      - name: Copy Slack notifications module
        run: |
          if [ -f "slack_notifications.py" ]; then
            mkdir -p agents
            cp slack_notifications.py agents/
            echo "✅ Copied slack_notifications.py"
          else
            echo "⚠️ slack_notifications.py not found"
          fi
      
      - name: Run QA Agent (without Slack notification)
        if: github.event.pull_request.number
        env:
          # Remove SLACK_WEBHOOK_URL so QA agent doesn't send notification yet
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          NOTION_METRICS_DB_ID: ${{ secrets.NOTION_METRICS_DB_ID }}
        run: |
          echo "🤖 Running QA Agent (test generation only)..."
          python agents/qa_agent.py
      
      - name: Run Generated Tests
        id: run_tests
        continue-on-error: true
        run: |
          if [ -f "tests/test_generated.py" ]; then
            echo "🧪 Running generated tests..."
            export PYTHONPATH="${PYTHONPATH}:$(pwd)"
            
            set +e
            pytest tests/test_generated.py -v --tb=short > test_results.txt 2>&1
            TEST_EXIT_CODE=$?
            set -e
            
            echo "exit_code=$TEST_EXIT_CODE" >> $GITHUB_OUTPUT
            
            # Show results
            echo "--- Test Results ---"
            cat test_results.txt
            echo "--------------------"
            
            # Parse results
            PASSED=$(grep -oP '\d+(?= passed)' test_results.txt | head -1 || echo "0")
            FAILED=$(grep -oP '\d+(?= failed)' test_results.txt | head -1 || echo "0")
            ERRORS=$(grep -oP '\d+(?= error)' test_results.txt | head -1 || echo "0")
            
            echo "passed=$PASSED" >> $GITHUB_OUTPUT
            echo "failed=$FAILED" >> $GITHUB_OUTPUT
            echo "errors=$ERRORS" >> $GITHUB_OUTPUT
            
            TOTAL=$((PASSED + FAILED + ERRORS))
            echo "total=$TOTAL" >> $GITHUB_OUTPUT
            
            if [ $TOTAL -gt 0 ]; then
              SUCCESS_RATE=$(awk "BEGIN {printf \"%.1f\", ($PASSED / $TOTAL) * 100}")
            else
              SUCCESS_RATE="0.0"
            fi
            echo "success_rate=$SUCCESS_RATE" >> $GITHUB_OUTPUT
            
            echo "📊 Results: $PASSED passed, $FAILED failed, $ERRORS errors (Total: $TOTAL)"
          else
            echo "ℹ️ No test file found" > test_results.txt
            echo "exit_code=0" >> $GITHUB_OUTPUT
            echo "passed=0" >> $GITHUB_OUTPUT
            echo "failed=0" >> $GITHUB_OUTPUT
            echo "errors=0" >> $GITHUB_OUTPUT
            echo "total=0" >> $GITHUB_OUTPUT
            echo "success_rate=0.0" >> $GITHUB_OUTPUT
          fi
      
      - name: Send Slack Notification with Results
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PASSED: ${{ steps.run_tests.outputs.passed }}
          FAILED: ${{ steps.run_tests.outputs.failed }}
          ERRORS: ${{ steps.run_tests.outputs.errors }}
          TOTAL: ${{ steps.run_tests.outputs.total }}
          SUCCESS_RATE: ${{ steps.run_tests.outputs.success_rate }}
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "⚠️ SLACK_WEBHOOK_URL not configured - skipping notification"
            exit 0
          fi
          
          python3 << 'SLACK_SCRIPT'
          import os
          import requests
          import json
          
          webhook_url = os.environ['SLACK_WEBHOOK_URL']
          pr_number = os.environ['PR_NUMBER']
          pr_title = os.environ.get('PR_TITLE', 'Unknown PR')
          passed = int(os.environ.get('PASSED', 0))
          failed = int(os.environ.get('FAILED', 0))
          errors = int(os.environ.get('ERRORS', 0))
          total = int(os.environ.get('TOTAL', 0))
          success_rate = float(os.environ.get('SUCCESS_RATE', 0.0))
          
          # Determine status
          if total == 0:
              status = "⏭️ No Tests"
              color = "#808080"
          elif failed == 0 and errors == 0:
              status = "✅ All Passed"
              color = "#36a64f"
          elif success_rate >= 80:
              status = "⚠️ Some Failures"
              color = "#ff9900"
          else:
              status = "❌ Many Failures"
              color = "#ff0000"
          
          # Build Slack message
          message = {
              "attachments": [
                  {
                      "color": color,
                      "blocks": [
                          {
                              "type": "header",
                              "text": {
                                  "type": "plain_text",
                                  "text": f"🧪 QA Agent Complete: PR #{pr_number}"
                              }
                          },
                          {
                              "type": "section",
                              "text": {
                                  "type": "mrkdwn",
                                  "text": f"*{pr_title}*\n{status}"
                              }
                          },
                          {
                              "type": "section",
                              "fields": [
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Tests Generated:*\n📝 {total} tests"
                                  },
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Success Rate:*\n📊 {success_rate}%"
                                  },
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Passed:*\n✅ {passed}"
                                  },
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Failed:*\n❌ {failed}"
                                  }
                              ]
                          },
                          {
                              "type": "actions",
                              "elements": [
                                  {
                                      "type": "button",
                                      "text": {
                                          "type": "plain_text",
                                          "text": "View Pull Request"
                                      },
                                      "url": f"https://github.com/{os.environ.get('GITHUB_REPOSITORY', '')}/pull/{pr_number}"
                                  }
                              ]
                          }
                      ]
                  }
              ]
          }
          
          try:
              response = requests.post(
                  webhook_url,
                  json=message,
                  timeout=10
              )
              
              if response.status_code == 200:
                  print("✅ Slack notification sent successfully!")
                  print(f"   Status: {status}")
                  print(f"   Tests: {passed}/{total} passed ({success_rate}%)")
              else:
                  print(f"⚠️ Slack notification failed: {response.status_code}")
                  print(f"Response: {response.text}")
          except Exception as e:
              print(f"❌ Error sending Slack notification: {e}")
          
          SLACK_SCRIPT
      
      - name: Post to GitHub and Notion
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          PASSED: ${{ steps.run_tests.outputs.passed }}
          FAILED: ${{ steps.run_tests.outputs.failed }}
          ERRORS: ${{ steps.run_tests.outputs.errors }}
          TOTAL: ${{ steps.run_tests.outputs.total }}
          SUCCESS_RATE: ${{ steps.run_tests.outputs.success_rate }}
        run: |
          python3 << 'PYTHON_SCRIPT'
          import os
          import requests
          import re
          
          # Read test results
          try:
              with open('test_results.txt', 'r') as f:
                  test_results = f.read()
          except FileNotFoundError:
              test_results = "No test results available"
          
          # Get metrics from environment
          passed = int(os.environ.get('PASSED', 0))
          failed = int(os.environ.get('FAILED', 0))
          errors = int(os.environ.get('ERRORS', 0))
          total = int(os.environ.get('TOTAL', 0))
          success_rate = float(os.environ.get('SUCCESS_RATE', 0.0))
          
          # Get PR details
          pr_number = os.environ['PR_NUMBER']
          repo = os.environ['GITHUB_REPOSITORY']
          token = os.environ['GITHUB_TOKEN']
          
          headers = {
              "Authorization": f"Bearer {token}",
              "Accept": "application/vnd.github+json"
          }
          
          try:
              pr_response = requests.get(
                  f"https://api.github.com/repos/{repo}/pulls/{pr_number}",
                  headers=headers,
                  timeout=10
              )
              pr_title = pr_response.json().get('title', 'Unknown PR')
          except:
              pr_title = 'Unknown PR'
          
          # Post to GitHub
          gh_comment = "## 🧪 QA Agent Report\n\n"
          gh_comment += "**Automated Test Generation Complete**\n\n"
          gh_comment += "### 📊 Test Summary\n"
          gh_comment += f"- **Total Tests:** {total}\n"
          gh_comment += f"- **Passed:** ✅ {passed}\n"
          gh_comment += f"- **Failed:** ❌ {failed}\n"
          gh_comment += f"- **Errors:** 🔴 {errors}\n"
          gh_comment += f"- **Success Rate:** {success_rate}%\n\n"
          gh_comment += "<details>\n<summary>📋 Detailed Results</summary>\n\n```\n"
          gh_comment += test_results[:2000] + "\n```\n</details>\n\n"
          gh_comment += "---\n*Generated by QA Agent*"
          
          try:
              gh_response = requests.post(
                  f"https://api.github.com/repos/{repo}/issues/{pr_number}/comments",
                  headers=headers,
                  json={"body": gh_comment},
                  timeout=30
              )
              
              if gh_response.status_code == 201:
                  print("✅ Posted to GitHub successfully!")
              else:
                  print(f"⚠️ GitHub post failed: {gh_response.status_code}")
          except Exception as e:
              print(f"❌ Error posting to GitHub: {e}")
          
          # Post to Notion (if configured)
          notion_token = os.environ.get('NOTION_TOKEN')
          notion_db = os.environ.get('NOTION_DATABASE_ID')
          
          if notion_token and notion_db:
              print("📝 Posting to Notion...")
              
              notion_headers = {
                  "Authorization": f"Bearer {notion_token}",
                  "Content-Type": "application/json",
                  "Notion-Version": "2022-06-28"
              }
              
              notion_data = {
                  "parent": {"database_id": notion_db},
                  "properties": {
                      "Name": {
                          "title": [{"text": {"content": f"QA Report: {pr_title[:70]} ({success_rate}%)"}}]
                      },
                      "Agent Type": {
                          "select": {"name": "QA"}
                      },
                      "Issue/PR Number": {
                          "number": int(pr_number)
                      },
                      "Status": {
                          "select": {"name": "Complete"}
                      }
                  }
              }
              
              try:
                  notion_response = requests.post(
                      "https://api.notion.com/v1/pages",
                      headers=notion_headers,
                      json=notion_data,
                      timeout=30
                  )
                  
                  if notion_response.status_code == 200:
                      print(f"✅ Posted to Notion successfully!")
                      print(f"   Tests: {passed}/{total} passed")
                  else:
                      print(f"⚠️ Notion post failed: {notion_response.status_code}")
              except Exception as e:
                  print(f"❌ Error posting to Notion: {e}")
          
          PYTHON_SCRIPT
