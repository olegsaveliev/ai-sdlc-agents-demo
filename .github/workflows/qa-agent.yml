name: QA Agent - Auto Test Generation

on:
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch:

jobs:
  qa-agent:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.ref }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install anthropic requests pytest dnspython
          
          # Install app dependencies if requirements.txt exists
          if [ -f "requirements.txt" ]; then
            echo "📦 Installing from requirements.txt..."
            pip install -r requirements.txt
          fi

      - name: Copy Slack notifications module
        run: |
          if [ -f "slack_notifications.py" ]; then
            mkdir -p agents
            cp slack_notifications.py agents/
            echo "✅ Copied slack_notifications.py"
          else
            echo "⚠️ slack_notifications.py not found"
          fi
      
      - name: Run QA Agent (without Slack notification)
        if: github.event.pull_request.number
        env:
          # Remove SLACK_WEBHOOK_URL so QA agent doesn't send notification yet
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          NOTION_METRICS_DB_ID: ${{ secrets.NOTION_METRICS_DB_ID }}
        run: |
          echo "🤖 Running QA Agent (test generation only)..."
          python agents/qa_agent.py
      
      - name: Run Generated Tests
        id: run_tests
        continue-on-error: true
        run: |
          if [ -f "tests/test_generated.py" ]; then
            echo "🧪 Running generated tests..."
            export PYTHONPATH="${PYTHONPATH}:$(pwd)"
            
            set +e
            pytest tests/test_generated.py -v --tb=short > test_results.txt 2>&1
            TEST_EXIT_CODE=$?
            set -e
            
            echo "exit_code=$TEST_EXIT_CODE" >> $GITHUB_OUTPUT
            
            # Show results
            echo "--- Test Results ---"
            cat test_results.txt
            echo "--------------------"
            
            # Parse results
            PASSED=$(grep -oP '\d+(?= passed)' test_results.txt | head -1 || echo "0")
            FAILED=$(grep -oP '\d+(?= failed)' test_results.txt | head -1 || echo "0")
            ERRORS=$(grep -oP '\d+(?= error)' test_results.txt | head -1 || echo "0")
            
            echo "passed=$PASSED" >> $GITHUB_OUTPUT
            echo "failed=$FAILED" >> $GITHUB_OUTPUT
            echo "errors=$ERRORS" >> $GITHUB_OUTPUT
            
            TOTAL=$((PASSED + FAILED + ERRORS))
            echo "total=$TOTAL" >> $GITHUB_OUTPUT
            
            if [ $TOTAL -gt 0 ]; then
              SUCCESS_RATE=$(awk "BEGIN {printf \"%.1f\", ($PASSED / $TOTAL) * 100}")
            else
              SUCCESS_RATE="0.0"
            fi
            echo "success_rate=$SUCCESS_RATE" >> $GITHUB_OUTPUT
            
            echo "📊 Results: $PASSED passed, $FAILED failed, $ERRORS errors (Total: $TOTAL)"
          else
            echo "ℹ️ No test file found" > test_results.txt
            echo "exit_code=0" >> $GITHUB_OUTPUT
            echo "passed=0" >> $GITHUB_OUTPUT
            echo "failed=0" >> $GITHUB_OUTPUT
            echo "errors=0" >> $GITHUB_OUTPUT
            echo "total=0" >> $GITHUB_OUTPUT
            echo "success_rate=0.0" >> $GITHUB_OUTPUT
          fi
      
      - name: Send Slack Notification with Results
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PASSED: ${{ steps.run_tests.outputs.passed }}
          FAILED: ${{ steps.run_tests.outputs.failed }}
          ERRORS: ${{ steps.run_tests.outputs.errors }}
          TOTAL: ${{ steps.run_tests.outputs.total }}
          SUCCESS_RATE: ${{ steps.run_tests.outputs.success_rate }}
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "⚠️ SLACK_WEBHOOK_URL not configured - skipping notification"
            exit 0
          fi
          
          python3 << 'SLACK_SCRIPT'
          import os
          import requests
          import json
          
          webhook_url = os.environ.get('SLACK_WEBHOOK_URL')
          if not webhook_url:
              print("⚠️ No Slack webhook URL - skipping")
              exit(0)
          
          pr_number = os.environ.get('PR_NUMBER', '0')
          pr_title = os.environ.get('PR_TITLE', 'Unknown PR')
          
          # Safe integer conversion with defaults
          passed = int(os.environ.get('PASSED', '0') or '0')
          failed = int(os.environ.get('FAILED', '0') or '0')
          errors = int(os.environ.get('ERRORS', '0') or '0')
          total = int(os.environ.get('TOTAL', '0') or '0')
          
          # Safe float conversion
          success_rate_str = os.environ.get('SUCCESS_RATE', '0.0') or '0.0'
          try:
              success_rate = float(success_rate_str)
          except ValueError:
              success_rate = 0.0
          
          # Determine status
          if total == 0:
              status = "⏭️ No Tests"
              color = "#808080"
          elif failed == 0 and errors == 0:
              status = "✅ All Passed"
              color = "#36a64f"
          elif success_rate >= 80:
              status = "⚠️ Some Failures"
              color = "#ff9900"
          else:
              status = "❌ Many Failures"
              color = "#ff0000"
          
          # Build Slack message
          message = {
              "attachments": [
                  {
                      "color": color,
                      "blocks": [
                          {
                              "type": "header",
                              "text": {
                                  "type": "plain_text",
                                  "text": f"🧪 QA Agent Complete: PR #{pr_number}"
                              }
                          },
                          {
                              "type": "section",
                              "text": {
                                  "type": "mrkdwn",
                                  "text": f"*{pr_title}*\n{status}"
                              }
                          },
                          {
                              "type": "section",
                              "fields": [
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Tests Generated:*\n📝 {total} tests"
                                  },
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Success Rate:*\n📊 {success_rate}%"
                                  },
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Passed:*\n✅ {passed}"
                                  },
                                  {
                                      "type": "mrkdwn",
                                      "text": f"*Failed:*\n❌ {failed}"
                                  }
                              ]
                          },
                          {
                              "type": "actions",
                              "elements": [
                                  {
                                      "type": "button",
                                      "text": {
                                          "type": "plain_text",
                                          "text": "View Pull Request"
                                      },
                                      "url": f"https://github.com/{os.environ.get('GITHUB_REPOSITORY', '')}/pull/{pr_number}"
                                  }
                              ]
                          }
                      ]
                  }
              ]
          }
          
          try:
              response = requests.post(
                  webhook_url,
                  json=message,
                  timeout=10
              )
              
              if response.status_code == 200:
                  print("✅ Slack notification sent successfully!")
                  print(f"   Status: {status}")
                  print(f"   Tests: {passed}/{total} passed ({success_rate}%)")
              else:
                  print(f"⚠️ Slack notification failed: {response.status_code}")
                  print(f"Response: {response.text}")
          except Exception as e:
              print(f"❌ Error sending Slack notification: {e}")
          
          SLACK_SCRIPT
      
      - name: Post to GitHub and Notion
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          PASSED: ${{ steps.run_tests.outputs.passed }}
          FAILED: ${{ steps.run_tests.outputs.failed }}
          ERRORS: ${{ steps.run_tests.outputs.errors }}
          TOTAL: ${{ steps.run_tests.outputs.total }}
          SUCCESS_RATE: ${{ steps.run_tests.outputs.success_rate }}
        run: |
          python3 << 'PYTHON_SCRIPT'
          import os
          import requests
          from datetime import datetime
          
          # Read test results
          try:
              with open('test_results.txt', 'r') as f:
                  test_results = f.read()
          except FileNotFoundError:
              test_results = "No test results available"
          
          # Get metrics from environment with safe defaults
          passed = int(os.environ.get('PASSED', '0') or '0')
          failed = int(os.environ.get('FAILED', '0') or '0')
          errors = int(os.environ.get('ERRORS', '0') or '0')
          total = int(os.environ.get('TOTAL', '0') or '0')
          
          success_rate_str = os.environ.get('SUCCESS_RATE', '0.0') or '0.0'
          try:
              success_rate = float(success_rate_str)
          except ValueError:
              success_rate = 0.0
          
          # Get PR details
          pr_number = os.environ['PR_NUMBER']
          repo = os.environ['GITHUB_REPOSITORY']
          token = os.environ['GITHUB_TOKEN']
          
          headers = {
              "Authorization": f"Bearer {token}",
              "Accept": "application/vnd.github+json"
          }
          
          # Get PR title
          try:
              pr_response = requests.get(
                  f"https://api.github.com/repos/{repo}/pulls/{pr_number}",
                  headers=headers,
                  timeout=10
              )
              pr_title = pr_response.json().get('title', 'Unknown PR')
          except:
              pr_title = 'Unknown PR'
          
          # Post to GitHub
          gh_comment = "## 🧪 QA Agent Report\n\n"
          gh_comment += "**Automated Test Generation Complete**\n\n"
          gh_comment += "### 📊 Test Summary\n"
          gh_comment += f"- **Total Tests:** {total}\n"
          gh_comment += f"- **Passed:** ✅ {passed}\n"
          gh_comment += f"- **Failed:** ❌ {failed}\n"
          gh_comment += f"- **Errors:** 🔴 {errors}\n"
          gh_comment += f"- **Success Rate:** {success_rate}%\n\n"
          gh_comment += "<details>\n<summary>📋 Detailed Results</summary>\n\n```\n"
          gh_comment += test_results[:2000] + "\n```\n</details>\n\n"
          gh_comment += "---\n*Generated by QA Agent*"
          
          try:
              gh_response = requests.post(
                  f"https://api.github.com/repos/{repo}/issues/{pr_number}/comments",
                  headers=headers,
                  json={"body": gh_comment},
                  timeout=30
              )
              
              if gh_response.status_code == 201:
                  print("✅ Posted to GitHub successfully!")
              else:
                  print(f"⚠️ GitHub post failed: {gh_response.status_code}")
          except Exception as e:
              print(f"❌ Error posting to GitHub: {e}")
          
          # Post to Notion (if configured) - WITH CONTENT!
          notion_token = os.environ.get('NOTION_TOKEN')
          notion_db = os.environ.get('NOTION_DATABASE_ID')
          
          if notion_token and notion_db:
              print("📝 Posting to Notion with full content...")
              
              notion_headers = {
                  "Authorization": f"Bearer {notion_token}",
                  "Content-Type": "application/json",
                  "Notion-Version": "2022-06-28"
              }
              
              # Build content blocks for page body
              content_blocks = []
              
              # Header
              content_blocks.append({
                  "object": "block",
                  "type": "heading_2",
                  "heading_2": {
                      "rich_text": [{"type": "text", "text": {"content": "🧪 QA Test Report"}}]
                  }
              })
              
              # PR info
              content_blocks.append({
                  "object": "block",
                  "type": "paragraph",
                  "paragraph": {
                      "rich_text": [{"type": "text", "text": {"content": f"Pull Request: {pr_title}"}}]
                  }
              })
              
              # GitHub link
              pr_url = f"https://github.com/{repo}/pull/{pr_number}"
              content_blocks.append({
                  "object": "block",
                  "type": "paragraph",
                  "paragraph": {
                      "rich_text": [
                          {"type": "text", "text": {"content": "View PR: "}},
                          {"type": "text", "text": {"content": pr_url, "link": {"url": pr_url}}}
                      ]
                  }
              })
              
              # Divider
              content_blocks.append({
                  "object": "block",
                  "type": "divider",
                  "divider": {}
              })
              
              # Test Summary Header
              content_blocks.append({
                  "object": "block",
                  "type": "heading_3",
                  "heading_3": {
                      "rich_text": [{"type": "text", "text": {"content": "📊 Test Summary"}}]
                  }
              })
              
              # Summary callout with color based on results
              if failed == 0 and errors == 0 and total > 0:
                  callout_color = "green"
                  callout_emoji = "✅"
              elif success_rate >= 80:
                  callout_color = "yellow"
                  callout_emoji = "⚠️"
              elif total == 0:
                  callout_color = "gray"
                  callout_emoji = "ℹ️"
              else:
                  callout_color = "red"
                  callout_emoji = "❌"
              
              summary_text = f"{callout_emoji} Total: {total} | Passed: {passed} | Failed: {failed} | Errors: {errors} | Success Rate: {success_rate}%"
              content_blocks.append({
                  "object": "block",
                  "type": "callout",
                  "callout": {
                      "rich_text": [{"type": "text", "text": {"content": summary_text}}],
                      "icon": {"emoji": "📈"},
                      "color": callout_color
                  }
              })
              
              # Test breakdown header
              content_blocks.append({
                  "object": "block",
                  "type": "heading_3",
                  "heading_3": {
                      "rich_text": [{"type": "text", "text": {"content": "Test Results Breakdown"}}]
                  }
              })
              
              # Passed tests
              if passed > 0:
                  content_blocks.append({
                      "object": "block",
                      "type": "bulleted_list_item",
                      "bulleted_list_item": {
                          "rich_text": [{"type": "text", "text": {"content": f"✅ Passed: {passed} test{'s' if passed != 1 else ''}"}}],
                          "color": "green"
                      }
                  })
              
              # Failed tests
              if failed > 0:
                  content_blocks.append({
                      "object": "block",
                      "type": "bulleted_list_item",
                      "bulleted_list_item": {
                          "rich_text": [{"type": "text", "text": {"content": f"❌ Failed: {failed} test{'s' if failed != 1 else ''}"}}],
                          "color": "red"
                      }
                  })
              
              # Errors
              if errors > 0:
                  content_blocks.append({
                      "object": "block",
                      "type": "bulleted_list_item",
                      "bulleted_list_item": {
                          "rich_text": [{"type": "text", "text": {"content": f"🔴 Errors: {errors} test{'s' if errors != 1 else ''}"}}],
                          "color": "red"
                      }
                  })
              
              # No tests case
              if total == 0:
                  content_blocks.append({
                      "object": "block",
                      "type": "callout",
                      "callout": {
                          "rich_text": [{"type": "text", "text": {"content": "No tests were generated or found for this PR"}}],
                          "icon": {"emoji": "ℹ️"},
                          "color": "gray"
                      }
                  })
              
              # Divider before metadata
              content_blocks.append({
                  "object": "block",
                  "type": "divider",
                  "divider": {}
              })
              
              # Metadata section
              content_blocks.append({
                  "object": "block",
                  "type": "heading_3",
                  "heading_3": {
                      "rich_text": [{"type": "text", "text": {"content": "📋 Metadata"}}]
                  }
              })
              
              # PR Number
              content_blocks.append({
                  "object": "block",
                  "type": "bulleted_list_item",
                  "bulleted_list_item": {
                      "rich_text": [
                          {"type": "text", "text": {"content": "PR Number: "}},
                          {"type": "text", "text": {"content": f"#{pr_number}", "annotations": {"bold": True}}}
                      ]
                  }
              })
              
              # Repository
              content_blocks.append({
                  "object": "block",
                  "type": "bulleted_list_item",
                  "bulleted_list_item": {
                      "rich_text": [
                          {"type": "text", "text": {"content": "Repository: "}},
                          {"type": "text", "text": {"content": repo, "annotations": {"code": True}}}
                      ]
                  }
              })
              
              # Timestamp
              timestamp = datetime.utcnow().strftime('%Y-%m-%d %H:%M UTC')
              content_blocks.append({
                  "object": "block",
                  "type": "bulleted_list_item",
                  "bulleted_list_item": {
                      "rich_text": [
                          {"type": "text", "text": {"content": "Generated: "}},
                          {"type": "text", "text": {"content": timestamp, "annotations": {"italic": True}}}
                      ]
                  }
              })
              
              # Test output section (if available)
              if test_results and test_results != "No test results available":
                  content_blocks.append({
                      "object": "block",
                      "type": "divider",
                      "divider": {}
                  })
                  
                  content_blocks.append({
                      "object": "block",
                      "type": "heading_3",
                      "heading_3": {
                          "rich_text": [{"type": "text", "text": {"content": "📄 Test Output"}}]
                      }
                  })
                  
                  # Truncate test results to fit Notion's limits (2000 chars per block)
                  truncated_results = test_results[:1800] if len(test_results) > 1800 else test_results
                  
                  if len(test_results) > 1800:
                      content_blocks.append({
                          "object": "block",
                          "type": "callout",
                          "callout": {
                              "rich_text": [{"type": "text", "text": {"content": "⚠️ Output truncated - view full results on GitHub"}}],
                              "icon": {"emoji": "⚠️"},
                              "color": "yellow"
                          }
                      })
                  
                  content_blocks.append({
                      "object": "block",
                      "type": "code",
                      "code": {
                          "rich_text": [{"type": "text", "text": {"content": truncated_results}}],
                          "language": "plain text"
                      }
                  })
              
              # Create the page with properties AND content
              notion_data = {
                  "parent": {"database_id": notion_db},
                  "properties": {
                      "Name": {
                          "title": [{"text": {"content": f"QA Report: {pr_title[:70]} ({success_rate}%)"}}]
                      },
                      "Agent Type": {
                          "select": {"name": "QA"}
                      },
                      "Issue/PR Number": {
                          "number": int(pr_number)
                      },
                      "Status": {
                          "select": {"name": "Complete"}
                      }
                  },
                  "children": content_blocks
              }
              
              try:
                  notion_response = requests.post(
                      "https://api.notion.com/v1/pages",
                      headers=notion_headers,
                      json=notion_data,
                      timeout=30
                  )
                  
                  if notion_response.status_code == 200:
                      print(f"✅ Posted to Notion successfully with full content!")
                      print(f"   Tests: {passed}/{total} passed")
                      print(f"   Content blocks: {len(content_blocks)}")
                  else:
                      print(f"⚠️ Notion post failed: {notion_response.status_code}")
                      print(f"   Response: {notion_response.text[:500]}")
              except Exception as e:
                  print(f"❌ Error posting to Notion: {e}")
          else:
              print("ℹ️ Notion not configured - skipping")
          
          print("\n" + "="*60)
          print("📊 Summary")
          print(f"   Total: {total} | Passed: {passed} | Failed: {failed} | Errors: {errors}")
          print(f"   Success Rate: {success_rate}%")
          print("="*60)
          
          PYTHON_SCRIPT
